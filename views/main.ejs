<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auron's System</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <div class="mainContainer">
        <div class="third hubContainer">
            <p>Hub</p>
            <div class="hubAccessContainer">
                <div class="hubAccessFirstSubContainer">
                <% quickAccessInstances.forEach(instance => {%>
                    <a href="<%=instance.link%>" class="quickAccessInstanceContainer">
                    <div>
                                                <p class="quickAccessInstanceText"><%=instance.text%></p>

                    </div>
                    <div style="background-image: url('<%= instance.img %>');"> </div>
                    </a>
                    <%})%>
                </div>
                <div class="hubAccessSecondSubContainer">
                                    <% secondaryAccessInstances.forEach(instance => {%>
                    <a href="<%=instance.link%>" class="quickAccessInstanceContainer">
                    <div>
                                                <p class="quickAccessInstanceText"><%=instance.text%></p>

                    </div>
                    <div style="background-image: url('<%= instance.img %>');"> </div>
                    </a>
                    <%})%>

                </div>
            </div>
            <div class="quickActionContainer">
                <div class="quickActionLabelAndButtonsContainer">
                    <div>Brainstorming</div>
                    <div>Quick Action Tab</div>
                    <div>Task Info</div>
                </div>
                <div class="quickActionTasksContainer">
                    <% quickActionTasks.forEach((task) => { %>
                        <div class="quickActionTask">
                            <p data-moveType="qa" data-id="<%=`taskId${task.id}`%>" data-position="<%=task.position%>" contenteditable="true"><%=task.content%></p>
                        </div>
                    <% }) %>
                </div>
                <div class="quickActionsSpacingDiv"></div>
            </div>
        </div>
        <div class="third projectsContainer">
            <div contenteditable="true" class="projectNameDiv">Project Name</div>
            <div contenteditable="true" class="projectNextTaskDiv">Next Task</div>
            <div contenteditable="true" class="projectTaskDateDiv">Sample Date</div>

        <% projectInstances.forEach((instance) => {%>
            <div data-moveType="pi" data-minimove="1" data-id="<%=instance.id%>" data-type="project_name" data-position="<%=instance.position%>" contenteditable="true" class="projectNameDiv"><%=instance.project_name%></div>
            <div data-moveType="pi" data-minimove="2" data-id="<%=instance.id%>" data-type="next_action" data-position="<%=instance.position%>" contenteditable="true" class="projectNextTaskDiv"><%=instance.next_action%></div>
            <div data-moveType="pi" data-minimove="3" data-id="<%=instance.id%>" data-type="example_date" data-position="<%=instance.position%>" contenteditable="true" class="projectTaskDateDiv"><%=instance.example_date%></div>
            <%})%>

        </div>
        <div class="third thirdContainer">Third Third</div>
    </div>
</body>
  <script src="/reload/reload.js"></script>
    <script src="js/main.js"></script>

</html>